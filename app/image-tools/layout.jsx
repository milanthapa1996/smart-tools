import React from "react";

const ImageToolsLayout = ({ children }) => {
  return (
    <main className="max-w-7xl mx-auto px-6">
      {children}
    </main>
  );
};

export default ImageToolsLayout;
